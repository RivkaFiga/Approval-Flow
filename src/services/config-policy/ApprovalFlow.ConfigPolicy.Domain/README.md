# ApprovalFlow.ConfigPolicy.Domain

Pure business types and rules for the **ConfigPolicy** service — no I/O, no framework,
no Dapr, no LLM. Unit-testable in isolation. References nothing (no shared Domain).
