# ApprovalFlow.Approval.Infrastructure

Adapters behind the Application ports for **Approval**: Dapr client
(state / pub-sub / secrets / workflow), Postgres repositories, external SDKs
(e.g. the LLM anti-corruption layer). Not shared with any other service.
